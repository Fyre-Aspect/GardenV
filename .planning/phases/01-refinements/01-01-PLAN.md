---
phase: 01-refinements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["src/components/dashboard.tsx", "src/components/garden/streak-dots.tsx", "src/components/garden-provider.tsx", "src/components/garden/plant-detail-dialog.tsx"]
autonomous: true
requirements: ["UI-01", "PLANT-01"]
must_haves:
  truths:
    - "User no longer sees emoji icons for score and streaks in the header"
    - "User can click a remove button to delete a plant from their garden"
  artifacts:
    - path: "src/components/dashboard.tsx"
      provides: "Clean header UI without emojis"
    - path: "src/components/garden-provider.tsx"
      provides: "removePlant state capability"
---

<objective>
Clean up the header UI by removing generic emoji icons and add the ability for end users to remove a plant from their tracked profile.
</objective>

<context>
@.planning/ROADMAP.md
@src/components/dashboard.tsx
@src/components/garden-provider.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean Dashboard Header UI</name>
  <files>src/components/dashboard.tsx, src/components/garden/streak-dots.tsx, src/components/garden/task-card.tsx</files>
  <action>
    Remove `Sparkles`, `Trophy`, and `Zap` lucide-react icons from the components.
    In `dashboard.tsx`, replace the XP, Streak and League indicators in the header with clean text equivalents or minimal abstract geometric shapes, purely removing the emojis.
    Remove `Sparkles` from the streak boxes. Make sure UI doesn't break due to missing icons.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Emoji icons are successfully removed and standard text stands in their place.</done>
</task>

<task type="auto">
  <name>Task 2: Enable Plant Removal Backend & State</name>
  <files>src/components/garden-provider.tsx</files>
  <action>
    Add a `removePlant: (id: string) => void` function to the `GardenContext`.
    Implement the deletion logic to filter out the plant from local `plants` array and make the delete request (or `setDoc` with filtered state) to Firebase storage matching the existing pattern.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Garden context exposes removePlant fn.</done>
</task>

<task type="auto">
  <name>Task 3: Plant Removal UI</name>
  <files>src/components/garden/plant-detail-dialog.tsx</files>
  <action>
    Add a "Remove Plant" button to the detail dialog (perhaps at the bottom or styled as a destructive button).
    When clicked, it should call `removePlant(plant.id)` from `useGarden` context and close the dialog via `onOpenChange(false)`.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Remove button exists and fires context function.</done>
</task>

</tasks>
