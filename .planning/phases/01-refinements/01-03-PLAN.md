---
phase: 01-refinements
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified: ["src/lib/data.ts", "src/components/garden-provider.tsx", "src/lib/plant-ai.ts"]
autonomous: true
requirements: ["HEALTH-01"]
must_haves:
  truths:
    - "Health is only determined by weekly photo checks, not daily watering"
    - "A weekly task requires the user to take a picture for a health check"
  artifacts:
    - path: "src/components/garden-provider.tsx"
      provides: "Health decoupling logic and weekly task generation"
---

<objective>
Revamp the health system so plant health is strictly a reflection of weekly photo checks rather than daily watering habits, reducing penalty friction.
</objective>

<context>
@.planning/ROADMAP.md
@src/lib/data.ts
@src/lib/plant-ai.ts
@src/components/garden-provider.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Disconnect Health from Daily Care</name>
  <files>src/lib/plant-ai.ts, src/components/garden-provider.tsx</files>
  <action>
    Remove any logic that penalizes `healthScore` based on intervals for 'water', 'fertilize', etc. Daily tasks should now only provide XP/streak progress, keeping `healthScore` independent.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Daily missed tasks no longer affect plant health.</done>
</task>

<task type="auto">
  <name>Task 2: Weekly Health Check Tasks</name>
  <files>src/lib/data.ts, src/components/garden-provider.tsx</files>
  <action>
    Add a new `CareType` like `'healthCheck'` or handle it via a new boolean on the task structure. 
    In `garden-provider.tsx`, implement logic to span a Weekly Health Check task for each plant once a week. 
    If this weekly task is neglected, THEN health can drop. Completing it requires "taking a picture" (simulate this by accepting completion when the image flow is triggered, or basically passing the task). 
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Weekly health tasks spawn and are the sole drivers of health checks.</done>
</task>

</tasks>
