---
phase: 01-refinements
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: ["src/lib/data.ts", "src/components/garden-provider.tsx", "src/components/garden/plant-detail-dialog.tsx", "src/components/garden/plant-card.tsx", "src/components/garden/task-card.tsx", "src/components/garden/plant-avatar.tsx"]
autonomous: true
requirements: ["PLANT-02", "DATA-01"]
must_haves:
  truths:
    - "User can assign a picture to a plant to provide context"
    - "Daily tasks tell the user exactly how much is needed (e.g. 200ml for water)"
  artifacts:
    - path: "src/lib/data.ts"
      provides: "Updated VM shapes"
    - path: "src/components/garden/task-card.tsx"
      provides: "Rendered amounts on tasks"
---

<objective>
Allow users to attach images to their plants for visual context, and provide precise daily care amounts in tasks rather than generic action labels.
</objective>

<context>
@.planning/ROADMAP.md
@src/lib/data.ts
@src/components/garden-provider.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Image Support to Plant Model</name>
  <files>src/lib/data.ts, src/components/garden/plant-detail-dialog.tsx, src/components/garden/plant-avatar.tsx</files>
  <action>
    Add `imageUrl?: string` to `PlantVM` in `data.ts`.
    In `plant-detail-dialog.tsx`, add UI to update this `imageUrl` via an input field (URL string is sufficient for prototype). Call an `updatePlant` context fn (similar to how things save) or handle local state.
    In `plant-avatar.tsx`, if `plant.imageUrl` exists, render an `img` tag instead of the text initial fallback.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>User can set an image URL on a plant and it renders as an avatar.</done>
</task>

<task type="auto">
  <name>Task 2: Task Requirements Specificity</name>
  <files>src/lib/data.ts, src/components/garden-provider.tsx, src/components/garden/task-card.tsx</files>
  <action>
    Add an optional `amount?: string` field to `TaskVM`.
    In `garden-provider.tsx` (where `tasks` are generated or mocked), provide mock specific amounts based on type (e.g., if type is 'water', amount: '200ml'). 
    Update `task-card.tsx` to render the amount alongside the type (e.g., "Water - 200ml") so the user knows EXACTLY what is needed.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Task cards display exact quantities required.</done>
</task>

</tasks>
