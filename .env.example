# Firebase Web App configuration.
# Where to find these: Firebase Console -> Project settings (gear icon) -> General tab ->
# "Your apps" -> select your Web app -> "SDK setup and configuration" -> Config.
#
# Copy this file to .env.local and fill in every value. .env.local is gitignored
# and must never be committed.

# Web API key (config.apiKey)
REACT_APP_FIREBASE_API_KEY=

# Auth domain, e.g. your-project.firebaseapp.com (config.authDomain)
REACT_APP_FIREBASE_AUTH_DOMAIN=

# Project ID (config.projectId)
REACT_APP_FIREBASE_PROJECT_ID=

# Storage bucket, e.g. your-project.appspot.com (config.storageBucket)
REACT_APP_FIREBASE_STORAGE_BUCKET=

# Cloud Messaging sender ID (config.messagingSenderId)
REACT_APP_FIREBASE_MESSAGING_SENDER_ID=

# App ID (config.appId)
REACT_APP_FIREBASE_APP_ID=
1